import React from 'react';
import DataUpload from './DataUpload';

const Home = () => {
    return (
        <div>
            <h2>Bienvenido al Observatorio de Violencia Hacia las Personas LGBTI+ de Honduras KAI+</h2>
            <DataUpload />
        </div>
    );
};

export default Home;
